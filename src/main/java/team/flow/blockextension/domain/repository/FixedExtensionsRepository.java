package team.flow.blockextension.domain.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import team.flow.blockextension.domain.entity.FixedExtensions;

public interface FixedExtensionsRepository extends JpaRepository<FixedExtensions,Long> {

}
